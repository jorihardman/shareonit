<%= remotipart_response do %>
  <% if @posting.errors.any? %>
    $('#new_posting_form').html('<%= escape_javascript(render 'postings/form') %>');
    resize_fancybox();
  <% else %>
    $.fancybox.close();
    $('#notice').html('Posting added.');
    $('#posting_list .list_element:first').before('<%= escape_javascript(posting_div @posting) %>');
    $('.buttonset').buttonset();
    $('a[rel*=facebox]').fancybox({
	    'transitionIn'	:	'elastic',
		  'transitionOut'	:	'elastic',
		  'showNavArrows': false,
		  'scrolling': 'no'
    });
  <% end %>
<% end %>