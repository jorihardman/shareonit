<%= remotipart_response do %>
  <% if @posting.errors.any? %>
    $('#new_posting_form').html('<%= escape_javascript(render :partial => 'postings/form') %>');
    resize_fancybox();
  <% else %>
    $.fancybox.close();
    $('#notice').html('Posting added.');
    $('#posting_list .list_element:first').before('<%= escape_javascript(raw(posting_div(@posting))) %>');
    $('.buttonset').buttonset();
    $('a[rel*=facebox]').fancybox({
	    'transitionIn'	:	'elastic',
		  'transitionOut'	:	'elastic',
		  'showNavArrows': false,
		  'scrolling': 'no'
    });
    $('tr td:first-child').truncate({max_length: 75});
  <% end %>
<% end %>
