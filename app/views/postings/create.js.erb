<%= remotipart_response do %>
  <% if @posting.errors.any? %>
    $('#new_posting_form').html('<%= j render('postings/form') %>');
    resize_fancybox();
  <% else %>
    $.fancybox.close();
    $('#notice').html('Posting added.');
    $('#posting_list .list_element:first').before('<%= j posting_div(@posting) %>');
    $('.buttonset').buttonset();
    $('a[rel*=facebox]').fancybox({
	    'transitionIn'	:	'elastic',
		  'transitionOut'	:	'elastic',
		  'showNavArrows': false,
		  'scrolling': 'no'
    });
  <% end %>
<% end %>