<% if @posting.errors.any? %>
  $('#edit_posting_form').html('<%= j render('postings/form') %>');
  resize_fancybox();
<% else %>
  $.fancybox.close();
  $('#notice').html('Posting updated.');
  $('#posting_<%= @posting.id %>').replaceWith('<%= j posting_div(@posting) %>');
  $('.buttonset').buttonset();
  $('a[rel*=facebox]').fancybox({
	  'transitionIn'	:	'elastic',
		'transitionOut'	:	'elastic',
		'showNavArrows': false,
		'scrolling': 'no'
  });
<% end %>
