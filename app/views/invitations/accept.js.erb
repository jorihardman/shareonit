$('#notice').html('Joined <%= escape_javascript @invitation.community.name %>.');
$('#invitation_<%= @invitation.id %>').remove();
$('#community_<%= @invitation.community.id %>').remove();
if ($('#invitation_list tr').length == 1) {
  $('#invitations').remove();
}
$('#user_communities .list_element:first').after('<%= escape_javascript community_div(@invitation.community) %>');
$('.buttonset').buttonset();