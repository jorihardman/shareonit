$('#notice').html('Invitation to <%= j @invitation.community.name %> rejected.');
$('#invitation_<%= @invitation.id %>').remove();
if ($('#invitation_list tr').length == 1) {
  $('#invitations').remove();
}
