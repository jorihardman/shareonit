$('#notice').html('Membership to <%= @membership.community.name %> requested. You will be notified via email when it is accepted.');
$('#community_<%= @membership.community_id %>').remove();
$('#my_communities tr:first').after('<%= raw(escape_javascript(community_table_row(@membership.community)))%>');
$('.buttonset').buttonset();
