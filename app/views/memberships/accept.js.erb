$('#notice').html('<%= j @membership.user.full_name %> accepted.');
$('#membership_<%= @membership.id %>').remove();
if ($('#requests tr').length == 1) {
  $('#requests').remove();
}
$('#members > table > tbody > tr:first').after('<%= j membership_table_row(@membership) %>');
$('.buttonset').buttonset();
