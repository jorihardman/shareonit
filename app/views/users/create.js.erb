<% if @user.errors.any? %>
  $('#new_user_form').html('<%= escape_javascript render(:partial => 'form') %>');
  resize_fancybox();
<% else %>
  $.fancybox.close();
  location.replace('<%= session[:return_to].nil? ? root_path : session[:return_to] %>');
<% end %>