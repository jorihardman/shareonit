<% if @user.errors.any? %>
  $('#new_user_form').html('<%= j render('users/form') %>');
  resize_fancybox();
<% else %>
  $.fancybox.close();
  window.location.replace('<%= session[:return_to].nil? ? root_path : session[:return_to] %>');
<% end %>