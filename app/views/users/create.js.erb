<% if @user.errors.any? %>
  $('#new_user_form').html('<%= escape_javascript(render :partial => 'users/form') %>');
<% else %>
  $.facebox.close();
  window.location.replace('<%= root_path %>');
<% end %>

